// FlashList / LegendList web stub — re-exports FlatList from react-native-web.
// @shopify/flash-list is used in 4 core screens.
// @legendapp/list is used in StreamsList and TabletStreamsLayout.
import { FlatList } from 'react-native';

export const FlashList = FlatList;
export const MasonryFlashList = FlatList;
export const LegendList = FlatList;
export default FlashList;
